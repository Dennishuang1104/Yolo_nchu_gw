train: datasets/Transportation-train/images  # 訓練集圖片目錄（相對於 path）
val: datasets/Transportation-train/images    # 驗證集圖片目錄（相對於 path）

nc: 3  # 類別數量 (car 和 bus)

names: ['car', 'bus', 'bicycle']  # 類別名稱列表